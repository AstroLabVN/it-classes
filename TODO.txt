cli 
  keyboard shortcuts
  move one word with crl
  delete with ctrl + w 
  delete with alt + backspace

scripting
  command : file
  basename / dirname


files
 cat vs less

- History command (history, Ctrl+R for reverse search) — very useful for beginners and fits naturally after keyboard shortcuts
- Exit/logout — exit, Ctrl+D — students should know how to close a shell session
- Command chaining basics — quick intro to ; and && (run two commands in sequence) since students will need this soon
- Error messages — a slide explaining how to read a typical bash error ("command not found", "permission denied", "no such file or directory") so students don't panic
- which command — pairs well with the built-in vs external discussion, and is more commonly used than type in practice


find
  regex
  basic batch operations


c'est le "Pourquoi ?" -> 
pourquoi j'ai besoin de ça ? quel problèm


==========================================================================
LESSONS IDEAS
==========================================================================
simple script

source file

ssh
  ssh into their linux vm

install apt package


basic keyboard shortcuts
  move cursors
  browser
  files : create dir, etc
  windows (set on the side, )


vars
vars substition
trap
redirection
https://8gwifi.org/tutorials/bash/practices-logging.jsp
history

https://saurabhshivde.hashnode.dev/day-4-90daysofdevops

● Your tmux server process is still running with old group memberships from before you were    
  added to the docker group. tmux reuses a long-running server process, so even new tmux       
  windows inherit the original groups. 


markdown
  links to titles in the same file ??
  2. [Architecture Overview](#architecture-overview):w

  size of image for logo ???
  use of an IDE (vs code)
==========================================================================



==========================================================================
WORKSHOP IDEAS
==========================================================================
keyboard shortcuts in general
browser : new tab, reopen closed tab, close tab, focus search bar, go to download, etc
==========================================================================
